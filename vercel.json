{
  "version": 2,
  "buildCommand": "cd CodeExplainer && npm install && npm run build",
  "outputDirectory": "CodeExplainer/dist",
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/api/$1"
    },
    {
      "src": "/(.*)",
      "dest": "/$1"
    }
  ]
}
